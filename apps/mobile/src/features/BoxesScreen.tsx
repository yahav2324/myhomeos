import { View, Text } from 'react-native';

export function BoxesScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Boxes (Feature)</Text>
    </View>
  );
}
